import { DemoCustomPipePipe } from './demo-custom-pipe.pipe';

describe('DemoCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DemoCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
